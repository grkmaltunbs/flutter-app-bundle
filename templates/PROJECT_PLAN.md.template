# <PROJECT_NAME> — project plan

Steps are **derived from `PRODUCT_SPEC.md`** — every flow and screen in the spec
maps to a step (or part of one). Each `## Step N` is one implementation session,
worked through in order via `/step`. Mark `[x]` when complete.

`/step` does not mark a step done until it has been **verified on the iOS
simulator** (demo flavor, fakes — no Firebase emulators): the new flow
plus every dependent flow are exercised, with zero runtime errors/exceptions
and zero render overflow across the responsive size matrix. Write Acceptance
criteria as **verifiable flow/state outcomes taken from the spec**.

Format per step:

```
## Step N — <Title>
- [ ] <!-- checkbox, flipped to [x] when done -->
- id: <kebab-slug>
- depends_on: <comma-separated step ids, or none>
- spec_refs: <flow/screen ids from PRODUCT_SPEC.md this step implements>

### Description
...what to build...

### Acceptance
- bullet list of verifiable criteria (happy path + error/edge paths + states
  from the spec; responsive on the size matrix; entitlement-gated if paid)

### Touchpoints
- expected files/dirs
```

---

## Step 0 — Bootstrap the project
- [ ]
- id: bootstrap
- depends_on: none

### Description

Set up the Flutter project scaffolding. `/init-app` already ran
`flutter create`, so the project structure exists.

- Replace the generated `pubspec.yaml` with the full dependency set
  from CLAUDE.md → Tech stack.
- Create the directory skeleton under `lib/`: `core/` and `features/_template/`.
- Add `analysis_options.yaml` extending `very_good_analysis`.
- Stub `bootstrap.dart`, `app.dart`, theme/router/logging/clock.
- Set up DI scaffolding and run `build_runner build`.
- Set up `l10n.yaml` and stub ARB files.

### Acceptance
- `flutter analyze` exits 0.
- `flutter test` exits 0.
- `dart run build_runner build` succeeds.
- App boots to a blank Material screen on an **iOS simulator** in the demo
  flavor (`--dart-define=APP_ENV=demo`) with fakes wired.
- No runtime errors/exceptions in the Dart MCP error log on boot.

### Touchpoints
- `pubspec.yaml`, `analysis_options.yaml`, `l10n.yaml`,
  `lib/main.dart`, `lib/app.dart`, `lib/bootstrap.dart`,
  `lib/core/**`, `lib/features/_template/**`,
  `lib/**/data/fakes/**` (demo-flavor fake repositories),
  `ios/*.storekit` (StoreKit test config, if monetised).

---

<!--
/init-app populates additional steps from PRODUCT_SPEC.md.
Expected shape:

  Step 1: Backend + DI + fakes layer (demo flavor wires fakes; real backend
          behind the same interfaces)
  Steps 2..N: Features in dependency order (one per spec flow/screen group)
  Step N+1: Responsive + accessibility pass (size matrix, textScale 2.0,
            zero overflow on all screens)
  Step N+2: Monetization wiring + entitlement gates (RevenueCat; .storekit +
            FakePurchaseRepository for simulator testing)
  Step N+3: Release prep
-->
