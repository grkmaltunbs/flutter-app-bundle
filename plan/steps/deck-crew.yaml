id: deck-crew
number: "16"
title: Deck crew — subagents as a strip, tool rows that open, and the line since you last looked
status: done
rank: 770
depends_on: [deck-on-the-phone]
meta: { qa_required: true }
gates:
  analyze: { at: 2026-09-06, note: "flutter analyze + dart analyze: no issues", status: passed}
  tests: { at: 2026-09-06, note: "kit 93 (parent_tool_use_id on every line, the task_* lines by kind, the transcript folding a subagent under its Agent row with progress\/doneAt\/report, crewOf per turn, 24 KB tool output kept and marked cut, the clock), app 121 (the crew chip at 1.0\/2.0\/3.12x with its rows folded and the sheet running then done, the Agent row opening the same member, a Bash row's sheet with the 24 KB note, the since-you-last-looked line above the first unread row, remembered on leaving and moved on return, another session all new, nothing remembered no line); the subagent protocol proven live on claude 2.1.261, 2026-09-06", status: passed}
  qa: { at: 2026-09-06, note: "Run by Claude on the user's phone over adb, 2026-09-06, on Nahmatik: a Task\/Agent turn (Explore on haiku) put an EXPLORE chip on the strip with a pulse and a running clock (0:03 → 0:08), its find\/wc rows stayed out of the main list; done, the chip dimmed and its tap showed DONE · IN 0:08 · 2 TOOL USES, the report, and its two rows; a tap on a Bash row there opened INPUT · BASH with the command and RESULT 398; the turn's row read 48.6K CTX · 663 OUT. A second turn with the app sent home after 2 s: on return the SINCE YOU LAST LOOKED line sat above the reply and the list landed on it. Sizes covered by tests; the 24 KB cut by tests.", status: passed}
sections:
  - title: Description
    body: |
      **Crew.** A `Task` tool use is a subagent; the stream tags every
      message it produces with `parent_tool_use_id`. The Deck shows each
      running subagent as a chip on a strip under the header — its
      description, its elapsed time, a working pulse — and folds its
      tool rows under the chip so the transcript stays the user's
      conversation. Done chips dim and carry the summary on tap. The
      constellation's session step keeps its waves; nothing else moves.

      **Tool rows that open.** A tap on any tool row shows the full input
      and the full result, bounded at 24 KB with "open on the Mac"
      beyond, in the mono face; the compact row stays as it is. Copy is
      the SelectionArea's.

      **Since you last looked.** The phone remembers the last row it
      showed with the app in front, per project; the next open draws a
      thin labelled line above the first unread row and lands the list
      on it; the finished-turn push opens there too. The marker moves
      when the user scrolls past it, and there is one, not one per turn.
  - title: Acceptance
    body: |
      - Send `/qa`; a flutter-qa chip appears with a running clock; its
        rows are under the chip, not in the main list; when it ends the
        chip dims and its summary is on tap.
      - Tap a Bash row: the command and the whole output; a 100 KB output
        is cut at 24 KB with the note.
      - Leave the app during a turn; come back: the line sits above the
        first row you have not seen, and the list starts there.
      - 1.0 / 2.0 / 3.12x: the strip wraps, nothing overflows.
  - title: QA walkthrough
    body: |
      1. Phone: "/qa" and watch the crew strip; tap the chip when done.
      2. Phone: background the app mid-turn; a push; open; the line.
  - title: Touchpoints
    body: |
      - `kit/lib/src/bridge.dart` (`parent_tool_use_id`), `app/lib/src/host/bridge_session.dart` (crew on the session doc), `app/lib/src/screens/deck_tab.dart` (strip, row sheet, marker), `app/lib/src/draft.dart` (last-seen)
history:
  - { at: "2026-09-04", event: added, note: "phase 3b — improve the deck" }
  - at: 2026-09-06
    event: started
  - at: 2026-09-06
    event: gate analyze passed
    note: "flutter analyze + dart analyze: no issues"
  - at: 2026-09-06
    event: gate tests passed
    note: "kit 93 (parent_tool_use_id on every line, the task_* lines by kind, the transcript folding a subagent under its Agent row with progress\/doneAt\/report, crewOf per turn, 24 KB tool output kept and marked cut, the clock), app 121 (the crew chip at 1.0\/2.0\/3.12x with its rows folded and the sheet running then done, the Agent row opening the same member, a Bash row's sheet with the 24 KB note, the since-you-last-looked line above the first unread row, remembered on leaving and moved on return, another session all new, nothing remembered no line); the subagent protocol proven live on claude 2.1.261, 2026-09-06"
  - at: 2026-09-06
    event: gate qa passed
    note: "Run by Claude on the user's phone over adb, 2026-09-06, on Nahmatik: a Task\/Agent turn (Explore on haiku) put an EXPLORE chip on the strip with a pulse and a running clock (0:03 → 0:08), its find\/wc rows stayed out of the main list; done, the chip dimmed and its tap showed DONE · IN 0:08 · 2 TOOL USES, the report, and its two rows; a tap on a Bash row there opened INPUT · BASH with the command and RESULT 398; the turn's row read 48.6K CTX · 663 OUT. A second turn with the app sent home after 2 s: on return the SINCE YOU LAST LOOKED line sat above the reply and the list landed on it. Sizes covered by tests; the 24 KB cut by tests."
  - at: 2026-09-06
    event: done
    note: "Subagents as chips on a strip with their rows folded under (Agent tool, parent_tool_use_id, task_* lines), every tool row opening to its whole input and result (24 KB), and the since-you-last-looked line per project on the phone"
