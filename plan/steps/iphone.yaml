id: iphone
number: "27"
title: The iPhone — the same app on the user's second phone, with pushes
status: active
rank: 1000
depends_on: [deck-on-the-phone, notifications]
meta: { qa_required: true }
gates:
  analyze: { at: 2026-09-13, note: flutter analyze clean; no formatting sweep., status: passed}
  tests: { at: 2026-09-13, note: 151 kit tests and 252 Flutter tests passed; 3 live bridge tests skipped by default. Six iOS action-channel tests plus phone build/registration regressions., status: passed}
  qa: { at: 2026-09-13, note: "iPhone 17 Pro simulator: 3 integration tests passed (native Firebase\/notification\/share initialization and sign-in UI; isolated scratch Deck start\/send\/permission\/question; Steps and Work draft persistence), no Dart exceptions\/overflow. Physical install, authenticated relay\/share and APNs delivery remain unverified\/open human items. Mac host built and relaunched.", status: passed}
sections:
  - title: Description
    body: |
      `flutter create --platforms ios` into `app/`, Firebase iOS app on
      `flutterappbundle`, the team's development profile, APNs through FCM,
      the notification actions as iOS categories. `ship.sh ios` installs to
      the connected iPhone. Nothing in the code is Android-only; this step is
      signing, capabilities and the two store-free installs.

      Try it on iOS (added 2026-09-04): a build for the iPhone goes over a
      wireless `flutter install -d <iphone>` to the paired phone from the
      host — no store, no TestFlight — and the share-sheet intake is the
      iOS share extension.
  - title: Acceptance
    body: |
      - The app runs on the iPhone; an ask arrives as a push with Allow.
      - `bash app/tool/ship.sh ios` builds and installs in one command.
      - TRY IT with the iPhone paired installs the project's app on it.
  - title: QA walkthrough
    body: |
      1. iPhone: sign in, open scratch, the constellation, an ask from the Mac.
      2. Simulator: run app/integration_test/iphone_test.dart for native startup
         and isolated scratch Deck, permission/question, Steps and Work flows.
      3. A physical paired iPhone proves TRY IT installation and APNs delivery
         after register-the-iphone and apns-key-for-iphone-pushes are closed.
  - title: Built 2026-09-13
    body: |
      iOS 15+ Runner and native share extension are implemented. Firebase uses
      the registered Apple app, APNs approval categories route through a
      queued native bridge, and absent APNs registration leaves the relay usable.
      TRY IT supports a single paired physical iPhone; ship.sh adds ios and
      ios-sim. The iPhone 17 Pro simulator passed all 3 integration flows:
      native bootstrap/sign-in UI, scratch Deck messaging/answers, Steps/Work.
      Physical installation, authenticated relay/share intake and remote APNs
      delivery still await the two open human items and device walkthrough.
  - title: Touchpoints
    body: |
      - `app/ios/**`, `app/tool/ship.sh`, `app/lib/firebase_options.dart`, `app/lib/src/host/builds.dart`
history:
  - { at: "2026-08-30", event: added }
  - { at: "2026-09-04", event: renumbered, note: "9 → 27; try-it on iOS folded in" }
  - at: 2026-09-13
    event: started
  - at: 2026-09-13
    event: gate tests passed
    note: 151 kit tests and 252 Flutter tests passed; 3 live bridge tests skipped by default. Six iOS action-channel tests plus phone build/registration regressions.
  - at: 2026-09-13
    event: gate analyze passed
    note: flutter analyze clean; no formatting sweep.
  - at: 2026-09-13
    event: gate qa passed
    note: "iPhone 17 Pro simulator: 3 integration tests passed (native Firebase\/notification\/share initialization and sign-in UI; isolated scratch Deck start\/send\/permission\/question; Steps and Work draft persistence), no Dart exceptions\/overflow. Physical install, authenticated relay\/share and APNs delivery remain unverified\/open human items. Mac host built and relaunched."
