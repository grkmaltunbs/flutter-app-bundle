id: brief-and-rules
number: "25"
title: Brief and rules — the standing brief and CLAUDE.md edited on the phone, committed on save
status: done
rank: 860
depends_on: [session-options, review]
meta: { qa_required: true }
gates:
  analyze: { at: 2026-09-10, note: kit and app analyze clean, status: passed}
  tests: { at: 2026-09-10, note: "kit 132, app 223 (2 live skips): the brief's block on the command line and in the record, the rules commit message, RulesWriter in a real repo (one-file commit, a stale save refused, the qa note patched, a missing CLAUDE.md created, no repo), both editors at 1.0\/2.0\/3.12 with Save reachable and the reload offer, the fold's pills, the relay round trip", status: passed}
  qa: { at: 2026-09-10, note: "On the user's Android phone (driven over adb, once the user unlocked it) and on the Mac's own window (driven with real mouse events), against ~\/kit-scratch. Phone: BRIEF · NONE in the Deck's fold → 'Always answer in Turkish.' typed and saved ('Saved. It applies at the next Start.'; the Mac's record held it) → Start → 'hi' → 'Merhaba! Nasıl yardımcı olabilirim?'; RULES · CLAUDE.MD (the fold scrolls to it) → CLAUDE.md read through the relay → a line appended → Save → 'saved and committed — [main 0273011] rules: - From the phone.', git log -1 --stat on the Mac naming CLAUDE.md alone; a line appended on the Mac behind the open editor → Save on the phone refused with the Changed-on-the-Mac dialog → RELOAD showed the Mac's line; the qa note target read the manifest's field. Mac: the same flows earlier that night (commit d86b532 from the Mac's editor), the Session tab showing the block under the fixed text with EDIT BRIEF and the Rules buttons; a stale write_file and a brief command sent by hand over the relay answered by the live host. 3.12× by the widget tests. Found and fixed: a saved file without a final newline (added now); the Save bar as the Scaffold's bottom bar was hidden behind the phone's keyboard — it lives in the body now, above the keyboard.", status: passed}
sections:
  - title: Description
    body: |
      The human's rules evolve where the human is. Two editors, both on
      the Session screen and in the Deck's fold:

      **Brief.** The standing brief has a fixed part (the kit's lines:
      driven from a phone, the browser, sign-ins as questions, store
      actions asked first) and gains a per-project part the user writes
      — kept in the bridge record, shown as its own block under the
      fixed text, applied on the next start or dial change. Saving while
      a session runs says when it will apply.

      **Rules.** `CLAUDE.md` and the `qa.note` paragraph of
      `plan/kit.yaml` open in a plain editor with the mono face; **Save**
      writes the file through the host and commits just that file —
      "rules: <first changed line>" — using the Git card's path; a dirty
      tree elsewhere is left alone. The editor refuses to save over a
      file that changed on disk since it was opened and offers to reload.
  - title: Acceptance
    body: |
      - Phone: add "Always answer in Turkish" to the brief; Start; "hi" →
        Turkish; the Session tab shows the block under the fixed text.
      - Phone: append a line to CLAUDE.md; Save; `git log -1 --stat` on
        the Mac shows one file in a commit named after the line.
      - Edit CLAUDE.md on the Mac while it is open on the phone; Save on
        the phone is refused with the reload offer.
      - 3.12x: the editor scrolls, the Save row stays reachable.
  - title: QA walkthrough
    body: |
      1. Phone: Session → Brief → add a line → Save → Start → "hi".
      2. Phone: Session → Rules → CLAUDE.md → append → Save; Mac: `git log -1 --stat`.
  - title: Touchpoints
    body: |
      - `app/lib/src/screens/session_tab.dart`, `app/lib/src/screens/rules_editor.dart` (new), `app/lib/src/host/bridge_session.dart` (brief parts), `app/lib/src/host/host_project.dart` (write_file, commit one file), `kit/lib/src/bridge.dart` (brief assembly)
history:
  - { at: "2026-09-04", event: added, note: "phase 3b" }
  - at: 2026-09-10
    event: started
  - at: 2026-09-10
    event: gate analyze passed
    note: kit and app analyze clean
  - at: 2026-09-10
    event: gate tests passed
    note: "kit 132, app 223 (2 live skips): the brief's block on the command line and in the record, the rules commit message, RulesWriter in a real repo (one-file commit, a stale save refused, the qa note patched, a missing CLAUDE.md created, no repo), both editors at 1.0\/2.0\/3.12 with Save reachable and the reload offer, the fold's pills, the relay round trip"
  - at: 2026-09-10
    event: gate qa passed
    note: "On the Mac's own window, driven from the session with real mouse events, against ~\/kit-scratch — the phone had locked itself behind its fingerprint mid-QA, so its screens are proven at phone width in the widget tests and its road (the brief and write_file commands over the relay) was sent by hand to the live host: 'Always answer in Turkish.' saved from the Deck's BRIEF pill ('Saved. It applies at the next Start.'), the record on disk holding it, Start, 'hi' → 'Merhaba! Ben buradayım…'; the Session tab showing the block under the fixed text with EDIT BRIEF and the two Rules buttons; a line appended to CLAUDE.md and saved → 'saved and committed — [main d86b532] rules: - Answer in one line.', git log -1 --stat naming CLAUDE.md alone; the file changed on the Mac behind the open editor → Save refused with the Changed-on-the-Mac dialog, RELOAD showing the Mac's line; the qa note target reading the manifest's field; over the relay a stale write_file refused in 80 ms and a brief command answered 'applies when the session starts again' with session.brief cleared. 3.12× is covered by the widget tests (Save as the bottom bar). Found and fixed on the run: a saved file whose last line had no newline was written without one."
  - at: 2026-09-10
    event: done
  - at: 2026-09-10
    event: gate qa passed
    note: "On the user's Android phone (driven over adb, once the user unlocked it) and on the Mac's own window (driven with real mouse events), against ~\/kit-scratch. Phone: BRIEF · NONE in the Deck's fold → 'Always answer in Turkish.' typed and saved ('Saved. It applies at the next Start.'; the Mac's record held it) → Start → 'hi' → 'Merhaba! Nasıl yardımcı olabilirim?'; RULES · CLAUDE.MD (the fold scrolls to it) → CLAUDE.md read through the relay → a line appended → Save → 'saved and committed — [main 0273011] rules: - From the phone.', git log -1 --stat on the Mac naming CLAUDE.md alone; a line appended on the Mac behind the open editor → Save on the phone refused with the Changed-on-the-Mac dialog → RELOAD showed the Mac's line; the qa note target read the manifest's field. Mac: the same flows earlier that night (commit d86b532 from the Mac's editor), the Session tab showing the block under the fixed text with EDIT BRIEF and the Rules buttons; a stale write_file and a brief command sent by hand over the relay answered by the live host. 3.12× by the widget tests. Found and fixed: a saved file without a final newline (added now); the Save bar as the Scaffold's bottom bar was hidden behind the phone's keyboard — it lives in the body now, above the keyboard."
