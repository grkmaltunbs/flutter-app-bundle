id: instruments
number: "15"
title: Instruments — the context gauge and the pool gauges on the Deck, and compaction offered before the wall
status: done
rank: 760
depends_on: [session-dials]
meta: { qa_required: true }
gates:
  analyze: { at: 2026-09-06, note: "flutter analyze + dart analyze: no issues", status: passed}
  tests: { at: 2026-09-06, note: "kit 89 (usage on assistant and result, both pool windows over the relay and back, the compaction lines, the window from modelUsage over the model name, the transcript's context\/turn cost\/compaction note, the readouts), app 115 (two arcs at 1.0\/2.0\/3.12x on a phone with INTERRUPT and the pill, the numbers on a tap, COMPACT only past 80 % and only idle, COMPACTING, the exhausted line; the host's session doc carries context\/pool\/compacting and \/compact goes as a message; the phone reads them and sends compact as a command); \/compact proven live headless on claude 2.1.261, 2026-09-06", status: passed}
  qa: { at: 2026-09-06, note: "Run by Claude on the user's phone over adb, 2026-09-06, on Nahmatik: Start from the phone, 'read lib\/ and summarise' — the context arc climbed 0 → 6 → 8 and the relay's session.context.used (79,363 of 1,000,000) equalled the CLI transcript's last assistant usage summed (32 + 7,487 + 71,844); the pool arc read 28 with '3 h 10 m' under it; the tap opened the numbers (79,363 \/ 1,000,000 tokens · 8 %, five-hour 28 % resets in 3 h 10 m (04:30), weekly 6 %); COMPACT from the sheet: COMPACTING… on the facts row, then 'Compacted · 79.7K → 9.7K tokens.' and the arc at 1, session.compacting true then false, used 9,675; the folded row kept both arcs. The exhausted line is covered by tests only.", status: passed}
sections:
  - title: Description
    body: |
      The state of the machine at a glance, in the Instrument skin's own
      language: two small arcs on the Deck header's facts row.

      **Context.** Every `assistant` message carries `usage`; the context
      in use is that message's `input_tokens + cache_creation_input_tokens
      + cache_read_input_tokens`. The window is the model's — 200k by
      default, 1M for the `[1m]` variants — from the init's model name.
      The arc fills; amber past 70 %, red past 85 %, the number on tap.
      Kept on the session doc (`context: {used, window, at}`) so both
      devices draw the same arc.

      **Pool.** `rate_limit_event` carries the type (`five_hour`,
      `seven_day`), the status and the reset time, and a utilization when
      the CLI sends one; the second arc is the five-hour pool with a
      countdown to reset, the weekly pool under it on the Session screen.
      "Exhausted · resets in 1 h 12 m" replaces the amber wait line when
      a turn is refused for the pool. Tokens per turn show on the result
      row; never dollars — `total_cost_usd` is list-price accounting, not
      a charge, and this app never suggests API billing.

      **Compaction.** Past 80 % the header offers **COMPACT**, which sends
      `/compact` as a user message — a spike the design note lists: if
      the pinned CLI compacts in `-p`, the arc drops and the row says so;
      if it does not, the offer reads "start a fresh session; this one
      is at n %" and Start with a new id is one tap. Record the answer in
      `DESIGN.md`.
  - title: Acceptance
    body: |
      - After a turn, the context arc's number equals the last assistant
        message's three usage fields summed (check against the Mac's
        process output).
      - The pool arc shows the reset countdown from the last
        `rate_limit_event`; a refused turn shows the exhausted line.
      - Above 80 %: COMPACT appears; below: it does not. Its result is
        whatever the spike found, and the row says which.
      - Both arcs read at 1.0 / 2.0 / 3.12x without overflow; tap shows the
        numbers.
  - title: QA walkthrough
    body: |
      1. Phone: Start; "read lib/ and summarise"; watch the context arc climb; tap it.
      2. Phone: the pool arc and its countdown; Mac: compare with the process output's rate_limit_event.
  - title: Touchpoints
    body: |
      - `kit/lib/src/bridge.dart` (usage, rate_limit parse), `app/lib/src/host/bridge_session.dart` (context on the session doc), `app/lib/src/screens/deck_tab.dart` (arcs), `app/lib/src/screens/session_tab.dart`, `app/lib/src/widgets/common.dart` (the arc), `app/DESIGN.md`
history:
  - { at: "2026-09-04", event: added, note: "phase 3b — the JARVIS reading" }
  - at: 2026-09-06
    event: started
  - at: 2026-09-06
    event: gate analyze passed
    note: "flutter analyze + dart analyze: no issues"
  - at: 2026-09-06
    event: gate tests passed
    note: "kit 89 (usage on assistant and result, both pool windows over the relay and back, the compaction lines, the window from modelUsage over the model name, the transcript's context\/turn cost\/compaction note, the readouts), app 115 (two arcs at 1.0\/2.0\/3.12x on a phone with INTERRUPT and the pill, the numbers on a tap, COMPACT only past 80 % and only idle, COMPACTING, the exhausted line; the host's session doc carries context\/pool\/compacting and \/compact goes as a message; the phone reads them and sends compact as a command); \/compact proven live headless on claude 2.1.261, 2026-09-06"
  - at: 2026-09-06
    event: gate qa passed
    note: "Run by Claude on the user's phone over adb, 2026-09-06, on Nahmatik: Start from the phone, 'read lib\/ and summarise' — the context arc climbed 0 → 6 → 8 and the relay's session.context.used (79,363 of 1,000,000) equalled the CLI transcript's last assistant usage summed (32 + 7,487 + 71,844); the pool arc read 28 with '3 h 10 m' under it; the tap opened the numbers (79,363 \/ 1,000,000 tokens · 8 %, five-hour 28 % resets in 3 h 10 m (04:30), weekly 6 %); COMPACT from the sheet: COMPACTING… on the facts row, then 'Compacted · 79.7K → 9.7K tokens.' and the arc at 1, session.compacting true then false, used 9,675; the folded row kept both arcs. The exhausted line is covered by tests only."
  - at: 2026-09-06
    event: done
    note: "Two arcs on both Decks (context against the model's window, the five-hour pool with its countdown), the numbers and COMPACT in a sheet, the weekly pool on the Session screen, a turn's tokens on its row; \/compact proven in -p (2.1.261) and recorded in DESIGN.md"
