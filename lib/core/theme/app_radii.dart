/// Corner-radius tokens ported from the design bundle (`styles.css`
/// `--r-sm/md/lg/xl`).
abstract final class AppRadii {
  /// Small radius — `--r-sm` (8px).
  static const double sm = 8;

  /// Medium radius — `--r-md` (14px).
  static const double md = 14;

  /// Large radius — `--r-lg` (22px).
  static const double lg = 22;

  /// Extra-large radius — `--r-xl` (28px).
  static const double xl = 28;
}
