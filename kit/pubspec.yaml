name: flutter_kit
description: The plan engine behind flutter-kit v2 — steps, gates, human items, and the views generated from them.
version: 2.0.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.6.0
  path: ^1.9.0
  yaml: ^3.1.2
  yaml_edit: ^2.2.1

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
