# flutter-kit app

The phase-2 shell: a Flutter app whose macOS/Windows build hosts the user's
own Claude Code and whose phone builds are remotes, relayed through the
Firebase project `flutterappbundle` (Firestore + Email/Password auth; one
user). Nothing here touches any consumer app's Firebase project.
